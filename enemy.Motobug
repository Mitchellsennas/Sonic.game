#pragma once
#include "Enemy.h"

class BuzzBomber : public Enemy {
public:
    BuzzBomber(sf::Vector2f start, float amplitude, float period, float xMin, float xMax);
    void update(float dt) override;
    void draw(sf::RenderWindow& w) override;
    sf::FloatRect bounds() const override;
    sf::Vector2f center() const override { return pos; }

private:
    sf::CircleShape body;
    sf::Vector2f base, pos;
    float amp, per, xL, xR;
    float t = 0.f, direction = 1.f, horizSpeed = 120.f;
};

